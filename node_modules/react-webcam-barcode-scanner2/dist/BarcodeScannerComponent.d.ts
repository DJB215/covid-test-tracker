import React from 'react';
import { Result } from 'zxing-library2';
declare const BarcodeScannerComponent: ({ width, height, onUpdate, inverse }: {
    width: number;
    height: number;
    onUpdate: (arg0: unknown, arg1?: Result) => void;
    inverse: boolean;
}) => React.ReactElement;
export default BarcodeScannerComponent;
